import { useState } from "react";
import { Link } from "react-router";
import { useAuth } from "../auth/AuthContext.js";
export function ForgotPasswordPage() { const { apiFetch } = useAuth(); const [email, setEmail] = useState(""); const [done, setDone] = useState(false); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); if (busy) return; setBusy(true); try { await apiFetch("/api/auth/forgot-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); setDone(true); } finally { setBusy(false); } } return <main className="page-shell auth-page"><h1>Recuperar senha</h1><form className="auth-form" onSubmit={submit}><label htmlFor="forgot-email">E-mail</label><input id="forgot-email" type="email" value={email} onChange={e => setEmail(e.target.value)} required /><button disabled={busy}>{busy ? "Enviando..." : "Gerar instruções"}</button>{done ? <p role="status">Se a conta existir, as instruções foram geradas.</p> : null}</form><Link to="/login">Voltar ao login</Link></main>; }
