export function Pagination({page,totalPages,onPage}:{page:number;totalPages:number;onPage:(page:number)=>void}){const visible=Math.max(1,totalPages);return <nav aria-label="Paginação"><button type="button" disabled={page<=1||totalPages===0} onClick={()=>onPage(page-1)}>Anterior</button><span>Página {totalPages===0?0:page} de {totalPages}</span><button type="button" disabled={totalPages===0||page>=totalPages} onClick={()=>onPage(page+1)}>Próxima</button></nav>}
